API_PORT=8787
CORS_ORIGIN=*

# Option A: Full URL (preferred when available)
# DATABASE_URL=postgresql://DB_USER:DB_PASSWORD@DB_HOST:5432/admin

# Option B: Split params
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=admin
DB_USER=postgres
DB_PASSWORD=change_me

# Set true only when your DB requires SSL
DB_SSL=false
